const initState = {
  cache: {},
  currentResult: {},
  searchString: '',
};

function reducer(state = initState, action) {
  switch (action.type) {
    default:
      break;
  }
  return state;
}

export default reducer;
